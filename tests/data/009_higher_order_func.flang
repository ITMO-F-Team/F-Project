(func compose (f g) (
    (return
        (lambda (x)
            (return (f (g x)))
        )
    )
))

(func inc (x)
    (return (add x 1))
)

(setq plusTwo (compose inc inc))

(setq plusFour (compose plusTwo plusTwo))

(assert (equal 5 (plusFour 1)))
